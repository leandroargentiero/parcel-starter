const Header = () => {
  const template = `
    <header>
      <h1>Parcel Starter Kit</h1>
      <p>This is a Starter boilerplate and JS bundler with Parcel, Babel and css preprocessing with Sass.</p>
    </header>
  `;

  return template;
};

export default Header;
